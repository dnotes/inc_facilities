<?php
// $Id$

/**
 * @file
 *
 */

function inc_facilities_install() {
  $theme_settings = theme_get_settings();
  $theme_overrides = array(
    'toggle_node_info_facility' => 0,
  );
  variable_set('theme_settings', array_merge($theme_settings, $theme_overrides));
}